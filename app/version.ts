// Injected by Vite from package.json at build time, so the About page and the
// service worker cache name cannot drift from the released version.
declare const __APP_VERSION__: string;
export const APP_VERSION: string = __APP_VERSION__;
